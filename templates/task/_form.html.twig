{{ form_start(form) }}
<div class="flex flex-col gap-5">
    {{ form_widget(form) }}
</div>
<div class="flex flex-col">
    <button class="btn btn-lg btn-accent w-full">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
